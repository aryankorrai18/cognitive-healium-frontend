# The healium pytest plugin auto-registers when the cognitive-healium
# package is installed (via the pytest11 entry point in pyproject.toml).
#
# No pytest_plugins line is needed here.
# This conftest only provides local test fixtures if required.
