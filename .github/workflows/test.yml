name: Cognitive Healium CI

on:
  # Fires only after the Pages deployment succeeds
  workflow_run:
    workflows: ["Deploy Frontend to GitHub Pages"]
    types:
      - completed
  # Also allow manual trigger for debugging
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"
  HEALIUM_BACKEND: "https://github.com/aryankorrai18/cognitive-healium-backend.git"

jobs:
  test-live-production:
    name: Production E2E Test (Live Site)
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.conclusion == 'success' ||
      github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout frontend repo
        uses: actions/checkout@v4

      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Cognitive Healium SDK from backend repo
        run: |
          pip install --upgrade pip
          pip install "cognitive-healium @ git+${{ env.HEALIUM_BACKEND }}"
          playwright install chromium --with-deps

      - name: Run Live Production Test
        env:
          GROQ_API_KEYS: ${{ secrets.GROQ_API_KEYS }}
          DEPLOYED_URL: https://aryankorrai18.github.io/cognitive-healium/
        run: |
          pytest tests/test_deployed_site.py -v --tb=short --healium-enabled
        # continue-on-error: true   # Uncomment if you don't want a broken UI to block PRs
